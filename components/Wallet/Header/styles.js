import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginVertical: 40,
        display: 'flex',
        gap: 2,
        alignItems: 'center',
    },
    title: {
        alignItems: 'center',
    },
    amount: {
        fontSize: 64,
        fontWeight: 'bold',
    },
    timer: {

    }
});