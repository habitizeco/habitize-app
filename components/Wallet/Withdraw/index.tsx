
import { StyleSheet, View, Image, Pressable, Text } from 'react-native';

import { Button } from '@/elements/Button';


export const WalletWithdraw = () => {
    return (
        <Button title="Withdraw Money" disabled={true} />
    );
}

